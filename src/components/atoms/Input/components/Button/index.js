import styled from '@emotion/styled';

const Button = styled.button`
  background: red;
  color: white;
  padding: 0 20px;
  font-weight: bold;
  cursor: pointer;
  height: 40px;
`;

export default Button;
